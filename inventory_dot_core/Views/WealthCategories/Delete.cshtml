@model inventory_dot_core.Models.WealthCategories
@using inventory_dot_core.Classes

@{
    ViewData["Title"] = $"{Dictionary.frasesDictionary["Delete"]} категория МЦ: {Model.Wcatname}";
}

<h1>@Dictionary.frasesDictionary["Delete"]</h1>

<h3>@Dictionary.frasesDictionary["DeleteConfirm"]</h3>
<div>
    <h4>Категория МЦ</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Wcatname)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Wcatname)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Wcatnotes)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Wcatnotes)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="WcatId" />
        <input type="submit" value="Удалить" class="btn btn-outline-danger"
               formmethod="post"
               asp-route-filter="@ViewBag.Filter"
               asp-route-sortExpression="@ViewBag.SortExpression"
               asp-route-page="@ViewBag.Page" /> |
        <partial name="_BackButton" />
    </form>
</div>
